services:
  - type: web
    name: swasthya-mitra
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn chatmodels.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false